services:
  web:
    build: .
    image: todo-app:latest
    env_file:
      - .env
    ports:
      - "80:8000"
    restart: unless-stopped

# Observação: o banco de dados é um RDS externo; não definimos serviço de DB aqui.
